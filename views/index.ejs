<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <a href="/new">New Message</a>
    <% messages.map((message) => { %>
        <h4> <%= message.user %> posted..</h4>
        <h1><%= message.text %></h1>
        <i><%= message.date %></i>
        <button>See Details</button>
    <% }) %>
</body>
</html>